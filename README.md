
  # Build Mini Food Ordering Hub

  This is a code bundle for Build Mini Food Ordering Hub. The original project is available at https://www.figma.com/design/xiyS3hPSDC2mTaD0UlCJ3U/Build-Mini-Food-Ordering-Hub.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  